# Plano: Dashboard de Controle de Milhas e Transferências

## Visão Geral

Aplicação web para controle pessoal de milhas/pontos em programas de fidelidade brasileiros, com gestão de transferências entre programas (incluindo bônus), assinaturas de fidelidade, alertas de expiração, metas, cotação e estatísticas.

## Stack Tecnológica

- **Framework:** Next.js 15 (App Router) + React 19 + TypeScript
- **Estilização:** Tailwind CSS + shadcn/ui
- **Backend/DB:** Supabase (PostgreSQL) — banco compartilhado, sem autenticação
- **Cliente Supabase:** `@supabase/supabase-js`
- **Gráficos:** Recharts
- **Forms/Validação:** React Hook Form + Zod
- **Ícones:** lucide-react
- **Datas:** date-fns
- **Estado/Cache:** TanStack Query (React Query)
- **Tabelas:** TanStack Table

## Estrutura do Projeto

```
miles-dashboard/
├── src/
│   ├── app/
│   │   ├── layout.tsx                # Layout raiz + sidebar
│   │   ├── page.tsx                  # Dashboard (home)
│   │   ├── titulares/page.tsx        # CRUD de titulares
│   │   ├── programas/page.tsx        # CRUD de programas + saldos
│   │   ├── transferencias/page.tsx   # Histórico + nova transferência
│   │   ├── assinaturas/page.tsx      # Assinaturas de fidelidade
│   │   ├── metas/page.tsx            # Metas de acúmulo
│   │   ├── cotacoes/page.tsx         # Cotação R$/milha
│   │   └── relatorios/page.tsx       # Gráficos detalhados
│   ├── components/
│   │   ├── ui/                       # shadcn/ui components
│   │   ├── layout/                   # Sidebar, Header
│   │   ├── dashboard/                # Cards, charts do dashboard
│   │   ├── programas/                # Forms e tabelas
│   │   ├── transferencias/           # Forms com cálculo de bônus
│   │   ├── assinaturas/              # Forms com cálculo de ROI
│   │   └── shared/                   # DataTable, EmptyState, etc
│   ├── lib/
│   │   ├── supabase/client.ts        # Cliente Supabase
│   │   ├── queries/                  # Hooks TanStack Query
│   │   ├── utils.ts                  # cn(), formatters
│   │   ├── calculations.ts           # Bônus, ROI, expiração
│   │   └── seed-programas.ts         # Pré-cadastro de programas
│   ├── types/                        # TypeScript types
│   └── hooks/
├── supabase/
│   └── migrations/                   # SQL schemas
├── .env.local                        # SUPABASE_URL e ANON_KEY
└── package.json
```

## Modelo de Dados (Supabase / PostgreSQL)

### `titulares`
- `id` uuid PK
- `nome` text
- `cpf` text (opcional)
- `email` text (opcional)
- `created_at` timestamptz

### `programas` (catálogo: pré-cadastrados + customizados)
- `id` uuid PK
- `nome` text (ex: "Smiles", "Latam Pass")
- `categoria` text (`aerea` | `cartao` | `bancario` | `varejo` | `hotel` | `outro`)
- `cor` text (hex para UI)
- `logo_url` text (opcional)
- `validade_meses` int (validade padrão das milhas)
- `is_default` boolean (pré-cadastrado)
- `created_at` timestamptz

### `contas` (instância de programa para um titular)
- `id` uuid PK
- `titular_id` uuid FK → titulares
- `programa_id` uuid FK → programas
- `numero_conta` text (opcional)
- `created_at` timestamptz
- (Saldo NÃO é coluna — calculado dinamicamente via SUM de `movimentacoes`)
- UNIQUE(titular_id, programa_id)

### `movimentacoes` (todo crédito/débito gera linha)
- `id` uuid PK
- `conta_id` uuid FK → contas
- `tipo` text (`credito` | `debito` | `transferencia_saida` | `transferencia_entrada` | `expiracao` | `assinatura` | `ajuste`)
- `quantidade` numeric (positivo = crédito, negativo = débito)
- `data` date
- `data_expiracao` date (NULL se não expira)
- `descricao` text
- `transferencia_id` uuid FK → transferencias (nullable)
- `assinatura_id` uuid FK → assinaturas (nullable)
- `created_at` timestamptz

### `transferencias`
- `id` uuid PK
- `conta_origem_id` uuid FK → contas
- `conta_destino_id` uuid FK → contas
- `quantidade_origem` numeric
- `quantidade_destino` numeric (após bônus/conversão)
- `bonus_percentual` numeric (ex: 100 = 100%)
- `taxa_conversao` numeric (ex: 1.0 = 1:1)
- `custo_reais` numeric (taxas em R$, opcional)
- `data` date
- `observacao` text
- `created_at` timestamptz

### `assinaturas` (clubes de fidelidade)
- `id` uuid PK
- `conta_id` uuid FK → contas
- `nome_plano` text (ex: "Smiles Clube 5000")
- `valor_mensal` numeric
- `dia_cobranca` int (1-31)
- `milhas_mensais` numeric (creditadas todo mês)
- `data_inicio` date
- `data_fim` date (nullable, ativa enquanto NULL)
- `status` text (`ativa` | `pausada` | `cancelada`)
- `created_at` timestamptz

### `metas`
- `id` uuid PK
- `conta_id` uuid FK → contas (nullable se meta global)
- `titular_id` uuid FK → titulares (nullable)
- `descricao` text
- `quantidade_alvo` numeric
- `data_alvo` date
- `concluida` boolean
- `created_at` timestamptz

### `cotacoes` (histórico de R$/milha por programa)
- `id` uuid PK
- `programa_id` uuid FK → programas
- `valor_milheiro` numeric (R$ por 1000 milhas)
- `data` date
- `fonte` text (opcional)

## Funcionalidades Detalhadas

### 1. Dashboard (Home)
- Cards de resumo: total de milhas (consolidado), valor estimado em R$, milhas a expirar nos próximos 90 dias, transferências do mês.
- Gráfico de pizza: distribuição de milhas por programa.
- Gráfico de linha: evolução de saldo total nos últimos 12 meses.
- Lista das próximas expirações.
- Lista de assinaturas ativas com próxima cobrança.

### 2. Titulares
- CRUD simples (nome, CPF, email).
- Filtro do dashboard por titular.

### 3. Programas
- Lista de programas (pré-cadastrados + customizados).
- Adicionar programa customizado.
- Vincular titular ao programa (criar `conta`) e definir saldo inicial (gera movimentação de ajuste).
- Pré-cadastro inicial via seed: Smiles, Latam Pass, TudoAzul, Livelo, Esfera, Iupp, LifeMiles, Membership Rewards (Amex), Pontos Itaú, Atacadão Pontos, Hilton Honors, Marriott Bonvoy.

### 4. Transferências
- Formulário: conta origem, conta destino, quantidade origem, % bônus, taxa de conversão (default 1:1), custo em R$.
- Cálculo automático: `quantidade_destino = quantidade_origem * taxa_conversao * (1 + bonus/100)`.
- Ao salvar: gera 2 movimentações (saída e entrada) + linha em `transferencias`.
- Histórico com filtros (programa, período, titular).

### 5. Assinaturas
- CRUD de planos de fidelidade.
- Cálculo de ROI: custo por milha = `valor_mensal / milhas_mensais`.
- Job manual "Gerar créditos do mês" (botão) — cria movimentações para assinaturas ativas no dia de cobrança.
- Próximas cobranças no dashboard.

### 6. Alertas de Expiração
- Movimentações de crédito carregam `data_expiracao` (calculada via `validade_meses` do programa, configurável por movimentação).
- View/query SQL agregando milhas por janela de expiração (30/60/90/180 dias).
- Badge visual no dashboard.

### 7. Cotações (R$/milha)
- Cadastro manual de cotação atual por programa.
- Histórico de cotações com gráfico.
- Valor estimado da carteira = soma de `saldo * cotacao_atual` por programa.

### 8. Metas
- Meta por conta ou global.
- Barra de progresso (saldo atual / quantidade_alvo).
- Projeção: com base na média de acúmulo dos últimos 3 meses.

### 9. Relatórios
- Gráfico de barras: acúmulo vs gasto por mês.
- Gráfico de linha: evolução por programa.
- Tabela de transferências com totais.
- Export CSV.

## Cálculos-Chave (`lib/calculations.ts`)

- `calcularTransferencia(qtdOrigem, taxaConversao, bonusPct) → qtdDestino`
- `custoPorMilha(valorMensal, milhasMensais) → R$/milha`
- `valorEstimadoCarteira(contas, cotacoes) → R$`
- `milhasExpirandoEmDias(movimentacoes, dias) → number`
- `projecaoMeta(saldoAtual, alvo, mediaMensal) → mesesRestantes`

## Implementação por Fases

### Fase 1 — Setup (1 sessão)
1. `npx create-next-app@latest miles-dashboard --typescript --tailwind --app`
2. Instalar shadcn/ui, init e adicionar componentes base (button, card, dialog, form, input, select, table, tabs, sonner).
3. Instalar dependências: `@supabase/supabase-js`, `@tanstack/react-query`, `@tanstack/react-table`, `recharts`, `react-hook-form`, `zod`, `@hookform/resolvers`, `date-fns`, `lucide-react`.
4. Configurar `.env.local` com Supabase URL e anon key.
5. Layout raiz com sidebar de navegação.

### Fase 2 — Banco e Seed
1. Criar projeto Supabase (passo manual do usuário).
2. Migration SQL com todas as tabelas + indexes.
3. Seed SQL com programas pré-cadastrados.
4. Cliente Supabase em `lib/supabase/client.ts`.
5. Tipos TypeScript gerados (`supabase gen types`).

### Fase 3 — CRUDs base
1. Titulares (lista + form).
2. Programas (lista + adicionar customizado).
3. Contas (vincular titular a programa, saldo inicial).

### Fase 4 — Movimentações e Transferências
1. Form de transferência com cálculo em tempo real.
2. Geração transacional das 2 movimentações.
3. Recalcular `saldo_atual` da conta (trigger SQL ou cálculo no client).
4. Histórico de transferências com filtros.

### Fase 5 — Assinaturas
1. CRUD de assinaturas.
2. Cálculo de ROI exibido no card.
3. Botão "Gerar créditos do mês" (idempotente — checa se já existe movimentação no mês).

### Fase 6 — Dashboard e Gráficos
1. Cards de resumo com queries agregadas.
2. Recharts: pizza por programa + linha de evolução.
3. Lista de próximas expirações.

### Fase 7 — Cotações, Metas, Alertas, Relatórios
1. Cadastro de cotações + cálculo de valor da carteira.
2. CRUD de metas + barra de progresso + projeção.
3. View de expirações por janela.
4. Página de relatórios + export CSV.

### Fase 8 — Polimento
1. Estados de loading/empty/error em todas as páginas.
2. Toasts de feedback (sonner).
3. Modo escuro (next-themes).
4. Responsividade mobile.
5. README com instruções de setup e variáveis de ambiente.

## Pontos de Atenção

- **Sem autenticação:** o banco é compartilhado e o uso é pessoal. Não publicar a anon key em repo público sem antes restringir RLS ou adicionar auth.
- **Saldo da conta:** **DECIDIDO** — cálculo dinâmico via `SUM(movimentacoes.quantidade)` agrupado por `conta_id`. O campo `saldo_atual` em `contas` será removido (ou usado apenas como cache opcional via view materializada futuramente).
- **Expirações:** cada crédito pode ter sua própria `data_expiracao`. Débitos consomem créditos pelo método FIFO (decisão a confirmar antes de implementar consumo automático).
- **Geração mensal de créditos de assinatura:** sem cron automático no setup proposto. Botão manual no dashboard ou via Supabase Edge Function (futuro).

## Entregáveis

- Repositório Next.js funcional rodando local.
- Migrations SQL para subir schema no Supabase.
- Seed de programas brasileiros.
- README com setup passo a passo.
- Dashboard navegável com todas as funcionalidades listadas.

## Próximos passos após aprovação do plano

1. Criar projeto Next.js e instalar dependências.
2. Configurar Supabase e aplicar migrations.
3. Implementar fases 3 a 8 sequencialmente, validando com você ao fim de cada fase.
